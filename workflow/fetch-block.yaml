apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: fetch-block-
spec:
  serviceAccountName: workflow
  # arguments:
  #   parameters:
  #     - name: xxx
  #       value: xxx
  workflowTemplateRef:
    name: fetch-block
